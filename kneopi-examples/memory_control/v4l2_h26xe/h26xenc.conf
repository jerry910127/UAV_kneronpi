[main]
enable_write = 0 # save encoded data to file
codec_type = 0 # 0: h.264, 1:h.265
ssm_reader_pin = vsrc_0_ifp_0
srb_writer_pin = venc_srb_3

# if need to resize stream to desired size.
#Resize_Width = 640
#Resize_Height = 480
